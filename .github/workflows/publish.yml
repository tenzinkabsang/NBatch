name: Publish to NuGet

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  SOLUTION_DIR: src
  NUPKG_DIR: nupkgs

jobs:
  publish:
    name: Pack & Publish
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Derive version from tag
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_ENV

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.x

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.x

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.x

      - name: Restore
        run: dotnet restore
        working-directory: ${{ env.SOLUTION_DIR }}

      - name: Build
        run: dotnet build -c Release --no-restore /p:ContinuousIntegrationBuild=true /warnaserror
        working-directory: ${{ env.SOLUTION_DIR }}

      - name: Test
        run: dotnet test -c Release --no-build --filter "Category!=Docker" 
        working-directory: ${{ env.SOLUTION_DIR }}

      - name: Pack
        run: dotnet pack -c Release --no-build -o ${{ env.NUPKG_DIR }} /p:Version=${{ env.VERSION }}
        working-directory: ${{ env.SOLUTION_DIR }}

      - name: Push to NuGet
        run: dotnet nuget push "*.nupkg" --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        working-directory: ${{ env.SOLUTION_DIR }}/${{ env.NUPKG_DIR }}

      - name: Push symbols to NuGet
        run: dotnet nuget push "*.snupkg" --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
        working-directory: ${{ env.SOLUTION_DIR }}/${{ env.NUPKG_DIR }}
